/**
 * A model properties group for returning limited device specification.
 */
export const GROUP_SIMPLIFIED_SPEC = 'simplified-spec';
/**
 * A model properties group marking the field to be never exposed for returning limited device specification.
 */
export const GROUP_NEVER_SIMPLIFIED_SPEC = `!${GROUP_SIMPLIFIED_SPEC}`;