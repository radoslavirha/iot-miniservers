/**
 * A model properties group for returning limited data for IoT devices.
 */
export const GROUP_IOT = 'iot';

/**
 * A model properties group marking the field to be never exposed for returning limited data for IoT devices.
 */
export const GROUP_NEVER_IOT = `!${GROUP_IOT}`;
