/**
 * External APIs this service calls. Each value must have a matching entry under
 * `externalApis` in the configuration.
 */
export enum ExternalApi {
    /** Public MIoT specification registry (miot-spec.org). */
    MiotSpec = 'MIOT_SPEC'
}
